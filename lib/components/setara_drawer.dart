import 'package:flutter/material.dart';

class SetaraDrawer extends StatelessWidget {
  const SetaraDrawer({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}